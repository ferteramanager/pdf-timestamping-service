{
  "name": "pdf-timestamping-service",
  "version": "1.0.0",
  "description": "PDF timestamping service using OpenTimestamps",
  "main": "index.js",
  "scripts": {
    "deploy": "serverless deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-sdk": "^2.1692.0",
    "javascript-opentimestamps": "^0.4.5",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "serverless": "^3.35.2"
  }
}
